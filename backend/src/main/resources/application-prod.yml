logging:
  level:
    root: WARN
    com.greenjuicehub: INFO        # log business logic của mình
    org.springframework.web: WARN
  # Railway thu thập stdout, không cần ghi file
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"